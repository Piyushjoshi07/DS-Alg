class Solution {
    public String reverseString(String s) {
            if(s.length()<=1)
            {
             return s,   
            }
        else
        {
            return reverseString(s.substring(1,s.length()))+s.charAt(0);
        }
        
    }
}
